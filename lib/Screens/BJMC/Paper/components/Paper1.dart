import 'package:flutter/material.dart';
class paper1 extends StatelessWidget {
  const paper1({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('paper1BJMC'),
    );
  }
}
class paper2 extends StatelessWidget {
  const paper2({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('paper2'),
    );
  }
}
class paper3 extends StatelessWidget {
  const paper3({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('paper3'),
    );
  }
}
class paper4 extends StatelessWidget {
  const paper4({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('paper4'),
    );
  }
}
class paper5 extends StatelessWidget {
  const paper5({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('paper5'),
    );
  }
}
class paper6 extends StatelessWidget {
  const paper6({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('paper6'),
    );
  }
}
