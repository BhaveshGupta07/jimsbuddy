import 'package:flutter/material.dart';
class playlist1 extends StatelessWidget {
  const playlist1({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('playlist1BJMC'),
    );
  }
}
class playlist2 extends StatelessWidget {
  const playlist2({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('playlist2'),
    );
  }
}
class playlist3 extends StatelessWidget {
  const playlist3({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('playlist3'),
    );
  }
}
class playlist4 extends StatelessWidget {
  const playlist4({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('playlist4'),
    );
  }
}
class playlist5 extends StatelessWidget {
  const playlist5({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('playlist5'),
    );
  }
}
class playlist6 extends StatelessWidget {
  const playlist6({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('playlist6'),
    );
  }
}
